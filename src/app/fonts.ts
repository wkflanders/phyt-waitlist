import { Akshar, Inconsolata } from "next/font/google";

export const akshar = Akshar({
    subsets: ['latin'],
    weight: ['400', '700'],
});

export const inconsolata = Inconsolata({
    subsets: ['latin'],
    weight: ['400', '700'],
});